import { TemplatedTextDirective } from './templated-text.directive';

describe('TemplateTranslateDirective', () => {
  it('should create an instance', () => {
    // const directive = new TemplatedTextDirective();
    // expect(directive).toBeTruthy();
  });
});
